{
  "name": "mov2mp4",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:ffmpeg-core": "node scripts/sync-ffmpeg-core.mjs",
    "dev": "npm run sync:ffmpeg-core && vite",
    "build": "npm run sync:ffmpeg-core && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.6",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
